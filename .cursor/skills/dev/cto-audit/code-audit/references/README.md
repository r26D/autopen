# Code audit references

Each file here is an **environment** (language, stack, or config family) for `dev/cto-audit/code-audit`.

- **`elixir.md`** — Elixir / Mix / Phoenix; categories include `idiomatic_elixir` (see its OUTPUT FORMAT).
- **`docker.md`** — Dockerfiles and Docker Compose; categories include `security`, `reproducibility`, `operability`, `agent_execution_risk`, etc. (see its OUTPUT FORMAT).
- **`typescript.md`** — TypeScript / JavaScript / Node.js; categories include `idiomatic_typescript` (see its OUTPUT FORMAT).

Add more as `references/<name>.md` and extend the selection table in the parent `code-audit` `SKILL.md`. Each reference must define its own **category** enum in OUTPUT FORMAT.
